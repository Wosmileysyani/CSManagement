@model IEnumerable<CSManagement.Models.Short_Course>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SC_NameTH)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SC_NameTH)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SC_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SC_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SC_ID })
        </td>
    </tr>
}

</table>
