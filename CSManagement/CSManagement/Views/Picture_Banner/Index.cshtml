@model IEnumerable<CSManagement.Models.Picture_Banner>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Picture_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Picture_Img)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Picture_Link)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Picture_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Picture_Img)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Picture_Link)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Picture_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Picture_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Picture_ID })
        </td>
    </tr>
}

</table>
