@model IEnumerable<CSManagement.Models.School>

@{
    ViewBag.Title = "Index";
    int i = 1;
}

<section class="content">
    <div class="box box-default">
        <div class="box-header with-border">
            <div class="row">
                <div class="col-md-12">
                    <h1 class="box-title">จัดการข้อมูลโรงเรียนมัธยมปลาย</h1>
                    <div style="float:right">
                        <a class="btn btn-success" href="@Url.Action("Create", "Schools")"><i class="fa fa-plus"></i> เพิ่มข้อมูลการทำงาน</a>
                    </div>
                </div>
            </div>
        </div>

        <div class="box-body">
            <table id="example1" class="table table-bordered table-striped" style="width:100%">
                <thead>
                    <tr>
                        <th style="text-align:center">ที่</th>
                        <th style="text-align:center">@Html.DisplayNameFor(model => model.SCH_Name)</th>
                        <th style="text-align:center">@Html.DisplayNameFor(model => model.SCH_Province)</th>
                        <th style="text-align:center">@Html.DisplayNameFor(model => model.SCH_Tel)</th>
                        <th style="text-align:center">จัดการข้อมูล</th>
                    </tr>
                </thead>

                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td style="text-align:center">@(i++)</td>
                            <td>@Html.DisplayFor(modelItem => item.SCH_Name)</td>
                            <td>@Html.DisplayFor(modelItem => item.SCH_Province)</td>
                            <td style="text-align:center">@Html.DisplayFor(modelItem => item.SCH_Tel)</td>
                            <td style="text-align:center">
                                <a class="btn btn-warning" href="@Url.Action("Edit", "Schools", new { id = item.SCH_ID })"><i class="fa fa-edit"></i>แก้ไข</a>
                                @*<a class="btn btn-info" href="@Url.Action("Details", "Schools", new { id = item.SCH_ID })"><i class="fa fa-list-ul"></i> รายละเอียด</a>*@
                                <a class="btn btn-danger" href="@Url.Action("Delete", "Schools", new { id = item.SCH_ID })"><i class="fa  fa-trash-o"></i> ลบ</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</section>

<link href="~/Content/DataTables/css/dataTables.bootstrap.min.css" rel="stylesheet" />
@section Scripts{
    <script src="~/Scripts/DataTables/jquery.dataTables.min.js"></script>
    <script src="~/Scripts/DataTables/dataTables.bootstrap.min.js"></script>

    <script>
        $(document).ready(function () {
            $('#example1').DataTable({
                scrollCollapse: true,
                autoWidth: false,
                responsive: true,
                language: {
                    url: "//cdn.datatables.net/plug-ins/1.10.19/i18n/Thai.json"
                },
                columnDefs: [{
                    targets: "datatable-nosort",
                    orderable: false
                }]
            });
        });
    </script>
}
