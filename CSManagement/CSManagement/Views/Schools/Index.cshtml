@model IEnumerable<CSManagement.Models.School>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SCH_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SCH_Province)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SCH_Tel)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SCH_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SCH_Province)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SCH_Tel)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SCH_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SCH_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SCH_ID })
        </td>
    </tr>
}

</table>
