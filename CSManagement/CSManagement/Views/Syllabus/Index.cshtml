@model IEnumerable<CSManagement.Models.Syllabu>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Sy_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Sy_Details)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Sy_Img)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Sy_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Sy_Details)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Sy_Img)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Sy_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Sy_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Sy_ID })
        </td>
    </tr>
}

</table>
